<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Tree view -->
    <record id="view_purchase_request_tree" model="ir.ui.view">
        <field name="name">purchase.request.tree</field>
        <field name="model">purchase.request</field>
        <field name="arch" type="xml">
            <tree string="Purchase Requests">
                <field name="name"/>
                <field name="request_date"/>
                <field name="requested_by"/>
                <field name="state"/>
            </tree>
        </field>
    </record>

    <!-- Form view -->
    <record id="view_purchase_request_form" model="ir.ui.view">
        <field name="name">purchase.request.form</field>
        <field name="model">purchase.request</field>
        <field name="arch" type="xml">
            <form string="Purchase Request">
                <sheet>
                    <group>
                        <field name="name" readonly="1"/>
                        <field name="request_date"/>
                        <field name="requested_by"/>
                        <field name="description"/>
                        <field name="state" readonly="1"/>
                    </group>
                    <footer>
                        <button name="action_submit" type="object" string="Submit" states="draft" class="btn-primary"/>
                        <button name="action_approve" type="object" string="Approve" states="to_approve" class="btn-success"/>
                        <button name="action_reject" type="object" string="Reject" states="to_approve" class="btn-danger"/>
                        <button name="action_done" type="object" string="Mark as Done" states="approved" class="btn-secondary"/>
                        <button string="Cancel" class="btn-secondary" special="cancel"/>
                    </footer>
                </sheet>
            </form>
        </field>
    </record>

    <!-- Action -->
    <record id="action_purchase_request" model="ir.actions.act_window">
        <field name="name">Purchase Requests</field>
        <field name="res_model">purchase.request</field>
        <field name="view_mode">tree,form</field>
    </record>
</odoo>
